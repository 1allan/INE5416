
(defun main()
    (write-string ("Board id:"))

    ; (setq fileName(read))
    
    ; (concatenate ’string "inputs/" fileName ".txt")

    ; (setq lista '())

    ; ;parse do arquivo (colocar dados na lista)
    ; (with-open-file (in "inputs/input.txt" :if-does-not-exist nil)
    ;     (when in
    ;         (loop for line = (read-line in nil)
    ;             while line do (push line lista) ;; push to the beggining of the file
    ;         )
    ;     )
    ; )

    ; putStrLn "Board id:"
    ; fileName <- getLine
    ; str <- readFile ("./boards/" ++ fileName ++ ".txt")
    ; putStrLn "Solving..."
    ; let board = parse str
    ; printBoard (solve board)
    ; return ()

)
with-open-file


(main)